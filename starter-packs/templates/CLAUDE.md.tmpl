# Claude Code Entry Point

Read and follow `AGENTS.md` as the primary working contract for this Starter Pack. Then read the root `README.md` and begin in the entry mode the user requested.

If no mode was specified, briefly explain the three available modes and recommend one based on whether `context/` and `evidence/` already contain material.

Do not duplicate or override the rules in `AGENTS.md`. The generated `library/` is read-only reference content; working artifacts belong in `outputs/` and the root ledgers.

# Weak Example

> Store all useful context in the vector database and refresh it nightly.

This provides no purpose, authority, states, correction propagation, permissions, retention, conflict, expiry, failure behavior, or owner.

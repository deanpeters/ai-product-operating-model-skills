# Weak Example

> We use two model vendors, so vendor risk is low.

This ignores shared cloud, retrieval, data, contracts, operational knowledge, behavior differences, migration evidence, and whether either alternative can actually carry the workload.

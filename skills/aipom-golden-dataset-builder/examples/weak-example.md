# Weak Example

> Export 100 recent successful cases and use the existing summaries as correct answers.

This selects success bias, assumes historical outputs are truth, and omits provenance, permissions, coverage, failures, disagreement, holdout discipline, and lifecycle rules.
